import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'module_recap',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
